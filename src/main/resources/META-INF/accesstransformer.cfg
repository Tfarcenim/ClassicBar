# ClassicBar access transformers
# MC 26.1 removed the public getExhaustionLevel()/setExhaustion(float) accessors from
# FoodData; the exhaustionLevel field is now private. ClassicBar needs to read it (Hunger
# overlay, SyncHandler) and write it on the client (MessageExhaustionSync), so expose the
# field directly. Names are un-obfuscated in MC 26.1.
public net.minecraft.world.food.FoodData exhaustionLevel
